# TOOLS.md - Agency Pack Notes

## Client creation
Use:

```bash
./packs/ai-ad-agency-pack/scripts/create-client-agent.sh "Client Name" whatsapp "GROUP_ID"
```

## Important
- Restart gateway only after explicit approval.
- Store client secrets only inside that client's `secrets/keys.md`.
- Keep client-specific memory in the client workspace.
