# TOOLS.md - Client Agent Local Notes

- Use `config.yaml` for client account details.
- Use `secrets/keys.md` for private tokens when present.
- Never print tokens or secrets in chat.
- For Meta Ads reporting, use `skills/client-ads-manager/`.
